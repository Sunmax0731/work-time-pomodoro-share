# AGENTS

Scope is only `work-time-pomodoro-share`. Do not edit sibling repos, Rank 1-40 repos, PICKUP files, or unrelated workspaces from this repo.

## Rules

- Use `codex/closed-alpha-release` as the single work branch for closed alpha changes.
- Merge to `main`, push `origin/main`, and leave no local or remote `codex/*` branch.
- Run `npm test` after implementation or docs changes.
- Keep text files UTF-8 + LF.
- Manual tests are not run by Codex; S+ is not allowed until user-run manual results are recorded.
